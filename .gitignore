# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-server
dist-ssr
*.local

# Secrets and local env (never commit)
.env
.env.local
.env.production
*.pem

# MCP config may contain API keys or cluster IDs — use .example as template
.cursor/mcp.json
.agents/mcp.json

# Accidental TypeScript emit under src (keep intentional JS like servers/test-db.js)
src/**/*.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
